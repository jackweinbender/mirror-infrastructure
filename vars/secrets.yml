# Encrypt with: sops --encrypt --age <age-key> vars/secrets.yml > vars/secrets.yml.enc
# Decrypt with: sops --decrypt vars/secrets.yml.enc
vault_tailscale_auth_key: "tskey-auth-REPLACE_ME"
# Add other secrets here as needed