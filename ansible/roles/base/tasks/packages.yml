---
- name: Upgrade installed packages
  ansible.builtin.apt:
    upgrade: safe
    update_cache: true
    cache_valid_time: 3600

- name: Ensure baseline packages are installed
  ansible.builtin.apt:
    name:
      - curl
      - git
    state: present
