---
# Host-specific dynamic routes for services outside Docker discovery.
services:
  traefik:
    volumes:
      - ./dynamic/homeassistant.yaml:/etc/traefik/dynamic/homeassistant.yaml:ro
      - ./dynamic/jellyfin.yaml:/etc/traefik/dynamic/jellyfin.yaml:ro
      - ./dynamic/proxmox.yaml:/etc/traefik/dynamic/proxmox.yaml:ro
